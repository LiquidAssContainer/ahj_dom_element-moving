test('Useless test', () => {
  expect(1 + 1).toBe(2);
});

// TODO: Everything

// import HoleSwitch from '../HoleSwitch';

// test('Expect error', () => {
//   expect(new HoleSwitch(1)).toThrow(Error('Need at least 2 holes'));
// });
